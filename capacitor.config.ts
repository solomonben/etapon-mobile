import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'eTapon-mobile',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
